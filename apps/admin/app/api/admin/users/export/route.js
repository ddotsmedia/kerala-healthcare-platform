import { getSession } from '@/lib/auth'; export async function GET(req) { const session = await getSession(); if (!session?.role?.includes('admin')) return Response.json({ error: 'Unauthorized' }, { status: 401 }); const csv = 'Name,Email,Role\n'; return new Response(csv, { headers: { 'Content-Type': 'text/csv' } }); }
